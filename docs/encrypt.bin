# openssl genrsa -out key.pem 4096
# openssl rsa -in key.pem -pubout > pub.pem

for counter in $(seq 0 8); do openssl pkeyutl -encrypt -inkey pub.pem -pubin -in "message$counter.txt" | base64 > "encrypted$counter.txt"; done